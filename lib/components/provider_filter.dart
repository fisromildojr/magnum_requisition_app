import 'package:flutter/material.dart';

class ProviderFilter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Filtro do Fornecedor'),
    );
  }
}
